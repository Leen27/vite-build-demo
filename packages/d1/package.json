{
  "name": "d1",
  "version": "0.0.1",
  "main": "dist/bundle.js",
  "types": "./dist/types/index.d.ts",
  "scripts": {
    "clean:dist": "rimraf dist",
		"build": "rollup -c",
    "build:types": "tsc -b ./tsconfig.types.json"
	}
}
